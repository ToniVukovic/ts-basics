let username: string = "Alice";
let age: number = 30;
let isAdmin: boolean = true;

let scores: number[] = [7, 5, 1];
let tupleExample: [string, number] = ["Alice", 11];

enum Role {
  Admin,
  Editor,
  Viewer,
}

let adminRole: Role = Role.Admin;

let input: string | number;
input = "hello";
input = 30;

let direction: "up" | "down";
direction = "up";
